<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @song.title %>
</p>

<p>
  <strong>Duration(seconds):</strong>
  <%= @song.duration %>
</p>
<p>
  <strong>Price:</strong>
  <%= number_to_currency(@song.price) %>
</p>

<%= link_to 'Edit', edit_album_song_path(@album, @song) %> |
<%= link_to 'Back', album_songs_path(@album) %> <br>
