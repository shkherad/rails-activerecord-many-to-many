<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @song.title %>
</p>

<p>
  <strong>Duration(seconds):</strong>
  <%= @song.duration %>
</p>
<p>
  <strong>Price:</strong>
  <%= number_to_currency(@song.price) %>
</p>

<%= link_to 'Edit', send("edit_#{@parent_resource}_song_path",@parent, @song) %> |
<%= link_to 'Back', send("#{@parent_resource}_songs_path",@parent) %> <br>
